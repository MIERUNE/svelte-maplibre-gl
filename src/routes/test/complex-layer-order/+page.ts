// E2E fixture: minimal repro of the layer-order pattern from
// /examples/complex (BackgroundLayer anchors + non-contiguous layer
// additions across siblings).
export const ssr = false;
export const prerender = false;
